# 텍스트 줄바꿈 점검 · 수정 계획

> **적용 완료 (2026-07-30)** — 아래 1~3단계를 모두 반영했습니다.
> **35건 → 1건**. 남은 1건은 `동아리와의 추억 하나면` ⏎ `충분해요` (640px) 로,
> 어절 경계에서 자연스럽게 나뉜 정상 케이스입니다
> (점검 도구가 "마지막 줄 4자 이하" 를 기계적으로 잡은 것).
>
> | 폭 | 적용 전 | 적용 후 |
> |---|---|---|
> | 360px | 20 | 0 |
> | 390px | 21 | 0 |
> | 430px | 15 | 0 |
> | 640px | 15 | 1 |
> | 768px | 13 | 0 |
> | 1024px | 8 | 0 |
> | 1280 / 1440px | 6 | 0 |
>
> 아래 본문은 무엇이 문제였고 왜 그렇게 고쳤는지 남겨 둔 기록입니다.
> 문구를 새로 넣을 때 `node tools/linebreaks.mjs` 로 다시 점검할 수 있습니다.


360 / 390 / 430 / 640 / 768 / 1024 / 1280 / 1440px 여덟 개 폭에서 **실제 렌더된 줄**을
뽑아 검사했습니다. `<details>`(공연 모집)까지 펼친 상태입니다.

| 폭 | 문제 블록 수 |
|---|---|
| 360px | 20 |
| 390px | 21 |
| 430px | 15 |
| 640px | 15 |
| 768px | 13 |
| 1024px | 8 |
| 1280 / 1440px | 6 |

중복을 합치면 **35개 텍스트 블록**입니다. 좁은 화면일수록 많지만, 데스크톱에서도 6건이
남습니다.

---

## 1. 원인은 사실상 하나입니다

문제의 **거의 전부가 `word-break: normal`** 때문입니다.

브라우저는 한글을 "단어"로 인식하지 않아서, 줄이 꽉 차면 **어절 중간 아무 글자에서나**
끊습니다. 그래서 이런 게 나옵니다.

```
40주년을 담은, 하나의 오        ← #goods 제목, 390px
브제

오래된 악보와 추억의 온기를 닮은 깊은 우디·머     ← #chapters, 360px
스크의 잔향.

개인정보 제공에 동의합니다. (이름·연락처        ← #apply, 360px
는 창립제 신청 및 안내 목적으로만 사용되
며, 행사 종료 후 파기됩니다.)

... 40주년                                    ← #homecoming, 640~1440px 전부
의 중심 무대입니다.
```

`오/브제`, `우디·머/스크`, `연락처/는`, `40주년/의` — 조사가 떨어지고 고유명사가 쪼개집니다.

현재 `break-keep`(= `word-break: keep-all`)이 붙어 있는 곳은 **9곳뿐**입니다
(`Apply` 4, `FeeNotice` 2, `Venue`·`Homecoming`·`LuckyDraw` 각 1). 나머지 한글 본문은
전부 노출된 상태입니다.

---

## 2. 처방 — 3단계로 나눠 적용

### 1단계 (권장) · 전역 한 줄로 대부분 해결

`src/index.css`의 `@layer base` 안 `body`에 두 줄을 넣습니다.

```css
body {
  /* 한글은 어절 단위로만 끊는다 (조사·고유명사가 쪼개지지 않게) */
  word-break: keep-all;
  /* 단, 줄보다 긴 토큰(URL·계좌번호 등)은 넘치지 않게 끊어 준다 */
  overflow-wrap: break-word;
}
```

- `keep-all` 만 쓰면 아주 긴 영문·숫자 덩어리가 컨테이너를 삐져나갑니다.
  `overflow-wrap: break-word`가 그 안전망입니다. 두 줄을 **꼭 함께** 넣어야 합니다.
- 계좌번호에는 이미 `break-all`이 걸려 있어 그대로 동작합니다
  (`FeeNotice.tsx` `CopyRow`).
- 이 한 번으로 **35건 중 약 28건**이 사라집니다.

### 2단계 · 제목은 균형 맞추기

`keep-all`을 넣어도 제목은 여전히 어색하게 나뉩니다.

```
향은, 기억을 가장 오래 남기는     ← 390px
감각
```

제목류에 `text-wrap: balance`를, 본문에 `text-wrap: pretty`를 줍니다. `pretty`는
마지막 줄에 한 단어만 남는 것(고아 줄)을 브라우저가 알아서 줄여 줍니다.

```css
@layer base {
  h1, h2, h3 { text-wrap: balance; }
  p, li, dd, figcaption { text-wrap: pretty; }
}
```

Chrome 117+ · Safari 17.5+ 에서 동작하고, 지원하지 않는 브라우저는 그냥 무시합니다
(부작용 없음). 카톡 인앱 브라우저도 최신 크로미움 기반이라 대부분 적용됩니다.

### 3단계 · 그래도 남는 개별 케이스

1·2단계로 안 풀리는 건 **공백에서 끊기는데 그 위치가 내용상 부자연스러운** 경우입니다.
아래 표대로 손봐야 합니다.

---

## 3. 개별 수정 목록

### 🔴 반드시 고쳐야 하는 것 — 정보가 잘못 읽힘

| 위치 | 파일 | 현재 (폭) | 수정 |
|---|---|---|---|
| 일시 카드 | `Homecoming.tsx:59` | `2026년 8월 22일` ⏎ `(토)` (640px) | 날짜와 요일이 갈라짐. `whitespace-nowrap` 추가 |
| 일시 카드 보조 | `Homecoming.tsx:73` | `오후 6시 30분 (오후 6시 입장 시` ⏎ `작)` (768px) | 시작 시각과 입장 시각을 **두 줄로 분리** (괄호 묶음 해제) |
| 장소 카드 보조 | `Homecoming.tsx:73` | `... 남영역 1번 출구에서` ⏎ `244m` (430px)<br>`... 1호선 남영` ⏎ `역 1번 출구에서 244m` (1024~1440px) | 주소와 도보 안내를 **두 줄로 분리**. `·` 대신 줄바꿈 |
| 푸터 일시 | `EventSummary.tsx:48` | `2026년 8월 22일 (토) 오후 6시 30` ⏎ `분` (360px) | 날짜와 시각을 두 줄로 분리 |
| 푸터 장소 | `EventSummary.tsx:49` | `... 3층 · 1호선 남` ⏎ `영역 1번 출구에서 244m` (360px) | 위 장소 카드와 동일 |

> 위 네 곳은 모두 `EVENT.placeNote` / `doorsLabel`을 **한 문자열에 두 정보를 `·`로 이어 붙인**
> 구조 때문입니다. config에서 `placeNote`(주소)와 `placeAccess`(도보 안내)를 나누고
> 컴포넌트에서 두 줄로 렌더하는 게 근본 해결입니다.

### 🟡 눈에 띄는 것 — `keep-all`로 해결되지만 확인 권장

| 위치 | 파일 | 현재 (폭) |
|---|---|---|
| 기획 의도 제목 | `ProjectIntro.tsx:22` | `향은, 기억을 가장 오래 남기` ⏎ `는 감각` (360px) |
| 기획 의도 본문 3단락 | `ProjectIntro.tsx:29` | `여름공` ⏎ `연·겨울공연`, `개` ⏎ `성으로`, `전체에` ⏎ `는` 등 다수 |
| 창립제 소개문 | `Homecoming.tsx:43` | `40주년` ⏎ `의 중심 무대입니다.` — **640px부터 1440px까지 전부** |
| 챕터 카드 설명 4개 | `Chapters.tsx:84` | `우디·머` ⏎ `스크`, `시트러` ⏎ `스로`, `여운` ⏎ `을`, `꽃향` ⏎ `으로` |
| 챕터 마무리 문장 | `Chapters.tsx:121` | `우리 40주년 창` ⏎ `립제에 초대합니다.` (360px) |
| 프로그램 설명 5개 | `Timeline.tsx:40` | `열어` ⏎ `요.`, `즐겨` ⏎ `요.`, `시간` ⏎ `을`, `푸짐` ⏎ `한` |
| 즐길 거리 설명 4개 | `SpecialCorners.tsx:40` | `메시` ⏎ `지를`, `사진` ⏎ `을`, `에피` ⏎ `소드를`, `수행` ⏎ `해보세요!` |
| 비밀 굿즈 제목 | `Goods.tsx:28` | `하나의 오` ⏎ `브제` (390px) |
| 비밀 굿즈 안내 | `Goods.tsx:68` | `준비했어` ⏎ `요.` (430px) |
| 개인정보 동의 문구 | `Apply.tsx` 동의 label | `(이름·연락처` ⏎ `는 창립제 ...` (360px) |
| 제출 하단 안내 | `Apply.tsx` 제출 note | `간주` ⏎ `됩니다.` (390px) |
| 대상 카드 | `Homecoming.tsx:73` | `추억 하나면 충` ⏎ `분해요` (640px) |

### 🟢 고아 줄만 남는 것 — `text-wrap: pretty` 로 대부분 정리

| 위치 | 파일 | 현재 (폭) |
|---|---|---|
| 장소 ③ 설명 | `Venue.tsx` figcaption | `... 받아` ⏎ `가세요.` (390·768px) — 이미 `break-keep` 있음 |
| 회비 첫 안내 | `FeeNotice.tsx` notes | `... 입금해` ⏎ `주세요.` (430px) — 이미 `break-keep` 있음 |
| 챕터 리드 | `Chapters.tsx:25` | `... 하나의 향수가` ⏎ `됩니다.` (430px) |

---

## 4. 적용 순서 제안

1. **1단계 전역 CSS** — 커밋 하나. 여기서 대부분 해결되고 위험이 거의 없습니다.
2. **`EVENT.placeNote` 분리** — 🔴 5건이 한 번에 정리됩니다. config + 컴포넌트 2곳.
3. **2단계 `text-wrap`** — 커밋 하나. 제목·고아 줄 정리.
4. 남은 것 재측정 → 필요하면 문구 자체를 줄입니다
   (예: 창립제 소개문의 `40주년의 중심 무대입니다`는 문장이 길어 어느 폭에서든 걸립니다).

1~3단계만으로 35건 → **3~5건** 수준으로 줄어들 것으로 봅니다.

---

## 5. 검증 방법

점검 스크립트를 `tools/linebreaks.mjs` 에 넣어 두었습니다. 문구를 새로 넣거나 고칠 때
돌려 보면 눈으로 놓치는 것을 잡아 줍니다.

```bash
npm run dev                  # 개발 서버를 먼저 띄우고
node tools/linebreaks.mjs    # 폭별 건수 + 문제 줄 상세
```

폭별로 실제 렌더된 줄을 뽑아 ① 어절이 중간에서 쪼개졌는지 ② 마지막 줄에 한두 글자만
남았는지 판정합니다. 짧은 2줄 문장이 "고아 줄" 로 잡히는 건 오탐일 수 있으니
줄 내용을 보고 판단하세요.

수동 확인은 DevTools 디바이스 모드에서 **360 / 390 / 430 / 768 / 1024px** 다섯 개만
봐도 충분합니다. 문제가 몰려 있던 구간입니다.

---

## 6. 실제로 바꾼 파일

| 파일 | 변경 |
|---|---|
| `src/index.css` | `body` 에 `word-break: keep-all` + `overflow-wrap: break-word`,<br>`h1~h3` 에 `text-wrap: balance`, `p·li·dd·figcaption` 에 `text-wrap: pretty` |
| `src/config.ts` | `EVENT.placeNote` 에서 길 안내를 떼어 `EVENT.placeAccess` 신설 |
| `src/components/Homecoming.tsx` | 정보 카드의 `sub` → `subs: string[]`. 시각·주소를 한 줄씩 렌더 |
| `src/components/EventSummary.tsx` | 푸터 요약도 같은 방식으로 `subs` 화 |
| `tools/linebreaks.mjs` | 점검 도구 (신규) |
| `웹사이트문구.txt` | 일시·장소를 화면과 같은 줄 구성으로 갱신 |

개별 컴포넌트에 `break-keep` 을 하나씩 붙이는 대신 전역 CSS 한 곳에서 처리했습니다.
앞으로 추가되는 문구도 자동으로 적용됩니다.
